# ============================================================
# Server-side Environment Variables (Backend / Netlify Functions)
# ============================================================
# Set this in Netlify Dashboard with Scope = "Functions"
GROQ_API_KEY=
GROQ_MODEL=openai/gpt-oss-120b
GROQ_ENDPOINT=https://api.groq.com/openai/v1/chat/completions

# ============================================================
# Optional Client-side Environment Variables (VITE_*)
# ============================================================
# Custom proxy endpoint if backend is hosted on a different URL (defaults to /api/cosmo)
# VITE_COSMO_ENDPOINT=/api/cosmo
# Set to "true" if you want to force offline-only context brain mode
# VITE_COSMO_OFFLINE=false
